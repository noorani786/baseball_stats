source 'http://rubygems.org'

gem 'standalone_migrations'

group :development do
  gem 'sqlite3', '1.3.5'
end

group :test do
end

group :production do
  # replace if using another database for prod (ex: postgres)
  gem 'sqlite3', '1.3.5'
end